version: "3.9"

services:
  api:
    build: .
    container_name: minierp-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped